function funct() {
    let inputWidth = prompt('width = ');
    let inputHeight = prompt('height = ');
    let a = parseInt(inputWidth);
    let b = parseInt(inputHeight);
    let Area = a*b;
    document.write('the area: ' + Area);
}